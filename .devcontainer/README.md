# Dev Containers

## Linux image (default)

Validates profile JSON and supports doc editing. **Cannot build or run GameCrate.**

## Windows host (full development)

Clone on Windows and run:

```powershell
.\tools\Enter-GameCrateDev.ps1 -Build -PublishGui
```

See [docs/DEVELOPMENT.md](../docs/DEVELOPMENT.md).
